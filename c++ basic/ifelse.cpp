#include<iostream>
using namespace std;
int main() {
    int x= 28;
    if (x<18) {
        cout<<"You are not old enough to join";
    }
    else if (x>27) {
        cout<<"You are not being able to join";
    }
    else {
        cout<<"You are accepted. Come in!";
    }
    return 0;
}
